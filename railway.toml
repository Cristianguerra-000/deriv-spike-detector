[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd deriv_analyzer && uvicorn backend:app --host 0.0.0.0 --port $PORT --no-access-log"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
